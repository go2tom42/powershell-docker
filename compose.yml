version: '2.1'
services:
  psripper:
        image: tom42/ps_youtube_dl
        container_name: psripper
        volumes:
            - /home/docker/configs/psripper:/PSripper/config
            - /media/WHERE TO SAVE FILES:/media
        restart: always
        
networks:
  default:
    external:
      name: nginx-proxy        
